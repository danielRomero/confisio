-url = @category.new_record? ? categories_path(@section.permalink) : category_path(@section.permalink, @category.permalink)
= form_for @category, url: url do |f|
  - if @category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@category.errors.count, "error")} prohibited this discount from being saved:"
      %ul
        - @category.errors.full_messages.each do |msg|
          %li= msg
  .col-xs-12.col-sm-6.col-md-4.col-lg-4
    .form-group
      = f.label :name, 'Nombre'
      = f.text_field :name, class: 'form-control'
    .form-group
      = f.label :description, 'Descripción'
      = f.text_area :description, class: 'form-control'
    .actions
      = f.submit 'Guardar', class: 'btn btn-primary'