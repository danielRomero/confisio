<div class="hidden-xs col-sm-6 col-md-3 col-lg-3 ">
  <div class="thumbnail">
    <%= link_to post_path(post.section.permalink, post.permalink) do %>
      <%= image_tag(post.banner_url(:mini_thumb) || 'http://via.placeholder.com/350x200') %>
    <% end %>
    <div class="caption">
      <h4><%= post.title %></h4>
      <p>
        <%= link_to('Ver más', post_path(post.section.permalink, post.permalink), role: 'button', class: 'btn btn-primary') %>
      </p>
    </div>
  </div>
</div>
