<div class="col-sm-6 col-md-4">
  <div class="thumbnail">
    <%= image_tag post.banner_url(:thumb) || 'http://via.placeholder.com/350x200' %>
    <div class="caption">
      <h3><%= post.title %></h3>
      <p><%= strip_html_tags(post.body)[0..300] %>...</p>
      <p>
        <%= link_to('Ver más', post_path(post.section.permalink, post.permalink), role: 'button', class: 'btn btn-primary') %>
      </p>
    </div>
  </div>
</div>
