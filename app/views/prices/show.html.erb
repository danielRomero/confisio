<div class="page-header">
  <h2>Lista de precios de <%= @section.title %><small><%= link_to('Pide cita online', Settings.reservations_url, title: 'Pide cita online') %> o por teléfono <%= Settings.phone %></small></h2>
  <%= render @section.prices %>
</div>
